export const apiUrl = "https://localhost:7070/api/v1";

export const apiLogin = `${apiUrl}/auth/login`;

export const apiRegister = `${apiUrl}/auth/register`;

export const apiRefreshToken = `${apiUrl}/auth/refresh-token`;

export const apiRevokeToken = `${apiUrl}/auth/revoke-refresh-token`;

export const apiVerification = `${apiUrl}/auth/verify`;

export const apiAuthScheme = "Bearer ";
